name: Build compy
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions: 
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Build compy
        run: |
          go install github.com/barnacs/compy@master
          tree .
      - name: Move compy
        run: |
          mv ~/go .
      - uses: stefanzweifel/git-auto-commit-action@v5
        with:
          file_pattern: go
          push_options: '--force'
